name: Hello-World

on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          path: main

      - name: Checkout tools repo
        uses: actions/checkout@v4
        with:
          repository: GitHUb-Action-learn
          path: https://github.com/SantoshMorla/GitHUb-Action-learn/
        
      
    
    
